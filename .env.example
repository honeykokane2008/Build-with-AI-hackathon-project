# Required for the hackathon's mandatory Google AI integration.
# Get a free key at https://aistudio.google.com/app/apikey
# Without this, the app falls back to a deterministic rule-based classifier
# so it stays demoable, but this MUST be set for the submission to satisfy
# the "all solutions must integrate Google AI" requirement.
GEMINI_API_KEY=
